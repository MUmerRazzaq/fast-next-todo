# =============================================================================
# Fast Next Todo - Frontend Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration (PUBLIC - exposed to browser)
# -----------------------------------------------------------------------------
# Backend API URL - this is exposed to the browser via NEXT_PUBLIC_ prefix
# Development: http://localhost:8000/api/v1
# Production: https://fast-next-todo-k3hj.vercel.app/api/v1
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# -----------------------------------------------------------------------------
# Better Auth Configuration (SERVER-SIDE ONLY)
# -----------------------------------------------------------------------------
# Auth secret for signing JWT tokens (minimum 32 characters)
# MUST match the backend BETTER_AUTH_SECRET exactly!
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-super-secret-key-minimum-32-characters-long

# URL where Better Auth is hosted (your frontend app URL)
# Development: http://localhost:3000
# Production: https://fast-next-todo.vercel.app
BETTER_AUTH_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Database Configuration (SERVER-SIDE ONLY - for Better Auth)
# -----------------------------------------------------------------------------
# PostgreSQL connection string for Better Auth session storage
# This is used server-side only by Better Auth, never exposed to browser
DATABASE_URL=postgresql://user:password@localhost:5432/fast_next_todo

# -----------------------------------------------------------------------------
# Optional: Internal API URL (SERVER-SIDE ONLY)
# -----------------------------------------------------------------------------
# Use this for server-to-server communication in containerized environments
# If not set, falls back to NEXT_PUBLIC_API_URL
# API_URL_INTERNAL=http://backend:8000/api/v1
